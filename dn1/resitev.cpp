#include "resitev.h"


int Resitev::obdelaj(const string & ukaz){
    if (ukaz[0] == 'v'){
        // vstavim v strukturo (TODO)
        // in vrnem nekaj
        return 0;
    } else{
        // izracunam (TODO)
        int odgovor = abs(1 - 2);
        return odgovor;
    }
}